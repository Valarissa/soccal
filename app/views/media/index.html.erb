<h1>Listing media</h1>

<table>
  <tr>
    <th>Shareable</th>
    <th>Shareable type</th>
    <th>Shared</th>
    <th>Shared type</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @media.each do |medium| %>
  <tr>
    <td><%= medium.shareable_id %></td>
    <td><%= medium.shareable_type %></td>
    <td><%= medium.shared_id %></td>
    <td><%= medium.shared_type %></td>
    <td><%= link_to 'Show', medium %></td>
    <td><%= link_to 'Edit', edit_medium_path(medium) %></td>
    <td><%= link_to 'Destroy', medium, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Medium', new_medium_path %>
